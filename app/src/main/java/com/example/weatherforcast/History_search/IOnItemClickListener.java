package com.example.weatherforcast.History_search;

import com.example.weatherforcast.current_City.CurrentWeather;

public interface IOnItemClickListener {
    void onItemClick(CurrentWeather currentWeather);
}
